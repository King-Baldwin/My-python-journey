temp = float(input("What is the temperature outside : "))
is_raining = False
if temp > 35 or temp < 0 or is_raining:
    print("The event is cancelled")
else :
    print("The even is still on the schedule")

#or logical operator for only one I mean if one of the conditions which are mentioned is wrong the output shows cancelled. Thus or = at least one condition should be true

